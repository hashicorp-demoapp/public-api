# Coffee queries.
extend type Query {
  coffee(coffeeID: String!): Coffee
  coffees: [Coffee!]!
}

type Coffee {
  id: ID!
  name: String!
  image: String!
  price: Float!
  ingredients: [Ingredient]
}